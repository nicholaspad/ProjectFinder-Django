{
    "builds": [{
        "src": "ProjectFinder/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": {
            "maxLambdaSize": "15mb"
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "ProjectFinder/wsgi.py"
    }]
}